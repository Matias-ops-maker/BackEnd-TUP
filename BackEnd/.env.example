# ===================================
# BACKEND - Variables de Entorno
# ===================================

# JWT Authentication
JWT_SECRET=tu_jwt_secret_aqui

# MercadoPago (Sandbox credentials)
MP_ACCESS_TOKEN=tu_access_token_sandbox
MP_PUBLIC_KEY=tu_public_key_sandbox

# Server Configuration
PORT=4000

# Database Settings (SQLite por defecto, opcional para MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASS=tu_contraseña
DB_NAME=repuestos_auto

# Frontend URL (para CORS - ajustar según tu deployment)
FRONTEND_URL=http://localhost:5173

# API Key (para protección de endpoints públicos)
API_KEY=mi_api_key_super_secreta
